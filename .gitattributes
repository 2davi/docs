.husky/** text eol=lf

# 모든 텍스트 파일: 저장소에 LF로 저장, 체크아웃 시에도 LF 유지
* text=auto eol=lf

# 바이너리는 절대 건드리지 않음
*.png binary
*.jpg binary
*.svg text eol=lf
*.ico binary
*.woff binary
*.woff2 binary

# Windows 전용 스크립트 CRLF (예외적)
*.bat text eol=crlf
*.cmd text eol=crlf